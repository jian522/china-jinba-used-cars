# 金霸二手车出口独立站 — 网站开发规范

> 版本：v2.0 | 更新日期：2026-09-02  
> 项目目录：`D:\二手车出口网站`  
> 线上域名：jinbacars.com

---

## 一、项目文件目录结构

```
D:\二手车出口网站\
│
├── index.html                  # 根目录入口（302重定向到 /en/）
├── 404.html                    # 自定义404页面
├── CNAME                       # 域名绑定配置（jinbacars.com）
├── robots.txt                  # 搜索引擎爬虫规则
├── sitemap.xml                 # 主站点地图（车辆详情页）
├── sitemap-images.xml          # 图片站点地图
├── feed.xml                    # RSS订阅源
├── favicon.svg                 # 网站图标
├── README.md                   # 项目说明（简洁版）
│
├── en\                         # 英文主站（默认语言）
│   ├── index.html              # 首页（轮播+库存卡片+市场+流程+FAQ）
│   ├── cars\                   # 车辆详情页（每辆车独立HTML）
│   │   └── {id}\index.html    # 例：en/cars/100/index.html
│   ├── about\index.html        # About Us 页面
│   ├── contact\index.html      # Contact 页面
│   ├── markets\                # 目标市场介绍
│   ├── privacy\                # 隐私政策
│   ├── terms\                  # 服务条款
│   ├── brands\                 # 品牌列表
│   ├── categories\             # 车型分类
│   ├── chat\                   # 在线客服/WhatsApp浮窗
│   └── guides\                 # 出口指南
│
├── zh\                         # 中文站（结构与 en/ 完全一致）
│   ├── index.html
│   ├── cars\{id}\index.html
│   └── ...（同上，dir="ltr"）
│
├── ru\                         # 俄文站（结构与 en/ 完全一致）
│   ├── index.html
│   ├── cars\{id}\index.html
│   └── ...（同上，dir="ltr"）
│
├── ar\                         # 阿拉伯文站（RTL布局）
│   ├── index.html
│   ├── cars\{id}\index.html
│   └── ...（同上，dir="rtl"，lang="ar"）
│
├── assets\                     # 公共样式与脚本
│   ├── jinba-2026.css          # 主样式表（当前版本）
│   ├── jinba-2026.js           # 主交互脚本
│   ├── jinba-global.css        # 全局基础样式
│   ├── jinba-global.js         # 全局工具函数
│   ├── v3.css / v4.css / v5.css # 历史样式版本（可归档）
│   ├── v3.js / v4.js / v5.js   # 历史脚本版本
│   └── vehicle-images.json     # 车辆图片清单索引
│
├── images\                     # 静态资源
│   ├── certs\                  # 资质证书图片
│   │   ├── certificates.png/.svg
│   │   ├── export-docs.png/.svg
│   │   └── markets-board.png/.svg
│   ├── flags\                  # 国旗图标
│   ├── og-image.jpg/.svg       # Open Graph 分享图
│   └── logo.svg                # 品牌Logo
│
├── uploads\                    # 上传媒体库
│   ├── cars\                   # 车辆图片（按ID分文件夹）
│   │   └── {id}\
│   │       ├── primary.jpg     # 主图（封面）
│   │       ├── 1.jpg ~ N.jpg   # 附图
│   │       └── photos.json     # 该车辆图片元数据
│   ├── carousel\               # 首页轮播图
│   └── testimonials\           # 客户评价头像/截图
│
├── data\                       # 数据文件
│   ├── vehicles.json           # 车辆清单（215条，含多语言字段）
│   ├── photo-audit.json        # 图片完整性审计结果
│   ├── photo-completion-queue.csv  # 待补图清单
│   └── import-template.csv     # 批量导入模板
│
├── cars\                       # 旧版车辆目录（已迁移至各语言子目录，可保留参考）
│   └── {id}\index.html
│
├── about\                      # 旧版About（已迁移至 en/about/）
├── contact\                    # 旧版Contact
│
├── admin\                      # 管理后台（本地静态版）
│   ├── index.html              # 后台首页
│   ├── login\                  # 登录页
│   ├── app.js                  # 后台交互脚本
│   ├── admin.css               # 后台样式
│   └── photo-coverage\         # 图片覆盖率统计
│
├── scripts\                    # 构建脚本（Python）
│   ├── build_v2.py             # 站点生成主脚本
│   ├── build_v2_optimized.py   # 优化版生成脚本
│   ├── build_v2_seo.py         # SEO增强版生成脚本
│   ├── vehicle_pipeline.py     # 车辆数据管道
│   ├── enrich_inventory.py     # 库存信息补充
│   ├── batch_photo_import.py   # 批量图片导入
│   ├── photo_audit.py          # 图片审计
│   ├── fix_photo_manifest_0902.py # 修复照片清单
│   ├── api_push.py             # API推送（预留）
│   ├── seo_content.py          # SEO内容生成
│   ├── seo_health.py           # SEO健康检查
│   └── submit_indexnow.py      # 搜索引擎提交
│
├── imports\                    # 数据来源目录
│   ├── 2026-08-che168\         # 二手车之家导出数据
│   │   └── vehicles.csv        # CSV原始数据（56条）
│   └── *.jpg                   # 原始图片素材
│
├── tools\                      # 辅助工具
│   ├── fix-routes.ps1          # PowerShell修复脚本
│   └── fix-routes.cmd          # CMD修复脚本
│
├── marketing\                  # 营销文档
│   └── marketing.md            # 营销策略备忘
│
├── .git\                       # Git版本控制
└── .workbuddy\                 # WorkBuddy工作区记忆
    └── memory\
        └── YYYY-MM-DD.md       # 每日工作日志
```

---

## 二、技术栈说明

### 2.1 前端技术栈

| 类别 | 技术 | 说明 |
|------|------|------|
| 框架 | 无框架（纯静态） | 直接生成静态 HTML 文件，零运行时依赖 |
| 样式 | CSS3 + Flexbox/Grid | 现代化布局，响应式设计 |
| 主样式表 | `assets/jinba-2026.css` | 当前生效版本，品牌色：深蓝 `#071827`→`#0D2944`，橙色 `#FF6B2C` 强调 |
| 交互脚本 | 原生 JavaScript | 无 jQuery 依赖，ES6+ |
| 多语言 | 手动 i18n | en/zh/ru/ar 四个独立目录，`lang` 属性 + `dir="rtl"`（阿拉伯语） |
| SEO | 手写 meta + sitemap | 每个页面独立 `<title>`、`<meta description>`、Open Graph 标签 |
| 字体 | Inter / Inter Tight | Google Fonts，备用系统字体 |

### 2.2 数据层

| 文件 | 格式 | 用途 |
|------|------|------|
| `data/vehicles.json` | JSON | 215条车辆记录，含 title_i18n/description_i18n 多语言字段 |
| `data/photo-audit.json` | JSON | 图片覆盖率审计结果 |
| `uploads/cars/{id}/photos.json` | JSON | 每辆车的图片清单 |

### 2.3 构建工具

| 脚本 | 语言 | 功能 |
|------|------|------|
| `scripts/build_v2.py` | Python 3 | 从 vehicles.json 生成所有语言站点 |
| `scripts/enrich_inventory.py` | Python 3 | 补充车辆描述、价格换算等 |
| `scripts/batch_photo_import.py` | Python 3 | 批量导入图片到 uploads/ |
| `scripts/photo_audit.py` | Python 3 | 检查图片完整性 |

### 2.4 品牌规范

| 元素 | 值 |
|------|------|
| 主色（深蓝） | `#071827` → `#0D2944` 渐变 |
| 强调色（橙） | `#FF6B2C` |
| WhatsApp色 | `#25D366` |
| 背景白 | `#FFFFFF` |
| 文字灰 | `#18191C` / `#6B7280` |
| 圆角 | 卡片 12px，按钮 8px |

---

## 三、网站部署与上线步骤

### 方案 A：Cloudflare Pages（推荐，免费+全球CDN）

#### 3.1 准备工作
- 注册 [Cloudflare](https://developers.cloudflare.com/) 账号
- 将域名 `jinbacars.com` 的 DNS 托管至 Cloudflare

#### 3.2 部署步骤

**步骤 1：连接 Git 仓库**
```
1. 登录 Cloudflare Dashboard → Workers & Pages → Create app → Pages
2. 连接 GitHub 仓库（将 D:\二手车出口网站 推送到 GitHub）
3. 设置构建命令：无（静态站点，直接上传）
4. 输出目录：填写 `.`（根目录）
5. 点击 Save and Deploy
```

**步骤 2：域名绑定**
```
1. Dashboard → Your Pages project → Settings → Domains
2. 添加自定义域名：jinbacars.com
3. Cloudflare 会自动配置 SSL（HTTPS）
4. 确保 DNS 中 A 记录指向：192.0.2.1（或 Cloudflare 提供的 IP）
```

**步骤 3：验证部署**
```
访问 https://jinbacars.com
- 应自动 302 重定向到 /en/
- 检查 en/zh/ru/ar 四站均可访问
- 检查车辆详情页链接正常
```

---

### 方案 B：Netlify（免费+简单）

#### 3.1 部署步骤

**步骤 1：拖拽部署（最简单）**
```
1. 登录 https://app.netlify.com
2. 将 D:\二手车出口网站 文件夹拖入 "Drop folders here" 区域
3. Netlify 自动生成临时 URL（如 xxx.netlify.app）
4. 在 Site Settings → Change site name 设置自定义名称
```

**步骤 2：绑定域名**
```
1. Domain Settings → Add custom domain → jinbacars.com
2. 按提示配置 DNS（CNAME 或 A 记录）
3. Netlify 自动签发 SSL 证书
```

**步骤 3：Git 集成（可选）**
```
1. 连接 GitHub 仓库
2. 设置 Build command：无
3. Publish directory：.
4. 启用 Automatic deploys
```

---

### 方案 C：Vercel（免费+快速）

#### 3.1 部署步骤

```
1. 登录 https://vercel.com
2. Import Project → 选择 GitHub 仓库
3. Framework Preset：Other
4. Build Command：无
5. Output Directory：.
6. Deploy
```

**域名绑定：** Dashboard → Settings → Domains → 添加 jinbacars.com

---

### 方案 D：传统虚拟主机 / VPS

#### 3.1 上传文件

```bash
# 使用 FTP/SFTP 上传整个项目到服务器 Web 根目录
# 推荐工具：FileZilla、WinSCP

# 连接信息（示例）
主机：ftp.jinbacars.com
用户：your_ftp_user
密码：your_password
远程路径：/public_html/ 或 /www/ 或 /var/www/html/
```

#### 3.2 Nginx 配置（VPS）

```nginx
server {
    listen 80;
    server_name jinbacars.com www.jinbacars.com;
    root /var/www/jinbacars;
    index index.html;

    # 强制 HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name jinbacars.com www.jinbacars.com;
    root /var/www/jinbacars;
    index index.html;

    ssl_certificate     /path/to/ssl/cert.pem;
    ssl_certificate_key /path/to/ssl/key.pem;

    # SEO：处理 /en/ 路径
    location /en/ {
        try_files $uri $uri/ /en/index.html;
    }
    location /zh/ {
        try_files $uri $uri/ /zh/index.html;
    }
    location /ru/ {
        try_files $uri $uri/ /ru/index.html;
    }
    location /ar/ {
        try_files $uri $uri/ /ar/index.html;
    }

    # 压缩
    gzip on;
    gzip_types text/css application/javascript image/svg+xml;
}
```

#### 3.3 Apache 配置（.htaccess）

```apache
# 强制 HTTPS
RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# 根目录重定向到 /en/
RewriteCond %{REQUEST_URI} ^/$
RewriteRule ^$ /en/ [L,R=302]
```

---

## 四、上线前检查清单

### 4.1 功能检查
- [ ] 首页四语言切换正常（en/zh/ru/ar）
- [ ] 车辆列表页显示正常（215辆车）
- [ ] 车辆详情页图片、价格、描述完整
- [ ] WhatsApp 按钮跳转正确（wa.me 链接）
- [ ] 联系表单/邮箱链接可用
- [ ] 轮播图自动播放
- [ ] 移动端响应式正常（375px、768px、1440px）

### 4.2 SEO 检查
- [ ] 每个页面 `<title>` 唯一且含关键词
- [ ] `<meta description>` 中英文各不同
- [ ] `hreflang` 标注正确（en/zh/ru/ar 互链）
- [ ] `sitemap.xml` 包含所有车辆详情页 URL
- [ ] `robots.txt` 未屏蔽重要页面
- [ ] Open Graph 图片正常（og:image）

### 4.3 性能检查
- [ ] 图片已压缩（WebP 优先，JPEG 备用）
- [ ] CSS/JS 已最小化
- [ ] Lighthouse 得分 > 90（Performance）

### 4.4 安全检查
- [ ] HTTPS 强制生效
- [ ] 无混装内容（HTTP 资源）
- [ ] 管理后台 `admin/` 已加密码保护（或限制 IP）

---

## 五、后续迭代建议

1. **CMS 集成**：考虑接入 Strapi / Directus 等 Headless CMS，方便非技术人员更新车辆信息
2. **后端服务**：如需在线询价、用户账户、订单管理，可接入 Supabase / Firebase
3. **搜索引擎提交**：定期运行 `scripts/submit_indexnow.py` 向 Bing/Google 提交更新
4. **数据分析**：接入 Google Analytics 4 或百度统计
5. **多语言自动化**：目前手动维护四语，后续可接入 DeepL API 自动翻译

---

*文档版本：v2.0 | 最后更新：2026-09-02*
