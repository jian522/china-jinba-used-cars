# jinbacars.com 优化与改版交付报告

> 日期：2026-08-28 ｜ 站点：https://jinbacars.com （GitHub Pages，jian522/china-jinba-used-cars）

## 一、第一轮：网站检查与优化（已完成并上线）

| 问题 | 修复 | 线上验证 |
|---|---|---|
| 全站 hreflang 标签丢失（构建脚本 f-string 中 `{{hre}}` 写成字面量） | 改回 `{hre}`，重建 792 页 | `/en/` 与车辆页均含 en/zh/ru/ar/x-default 5 条 ✓ |
| 图库照片未展示（图库索引与构建数据脱节，958 vs 214） | 构建时合并 `assets/vehicle-images.json` 并做文件存在性校验 | `/en/cars/7/` 展示 primary + 7007_1..5.jpg ✓ |

提交 c31b2d60，已上线。

## 二、第二轮 A：UI 重构 v5（已完成并上线）

- 新建 `assets/v5.css`：完整设计系统（深海蓝 `#0b2239` 主色 + 琥珀金 `#d97706` 强调、4px 间距 token、4 级圆角/阴影、1024/768/480px 响应式断点、focus-visible / prefers-reduced-motion / print 样式），覆盖全部 67 个模板 class。
- 行为层 `assets/v4.js` 未动，8 个行为钩子全部兼容。
- 修复 v4.css 遗留的 5 处重复声明 bug。
- 全站 792 页重建，所有页面统一引用 `v5.css?v=1`。
- 恢复了并行会话造成的 build_v2.py 编码损坏与版本回退（从 2f6f4783 提取）。

## 三、第二轮 B：二手车数据管道（已完成，待真实数据）

**合规边界**：二手车之家/瓜子等平台受 robots 与版权限制，不做爬虫抓取；改为「车商数据 + CSV 模板 + 本地图片」合规管道。

**新增文件**：
- `scripts/vehicle_pipeline.py`：校验（必填字段、JB-XXXX 格式、VIN 碰撞、6-9 张图、SHA-256 跨车去重）→ 图片规范化（≤1600px JPEG q82 progressive）→ 合并（按 stock_id 更新/新增，`_placeholder` 占位机制）→ 重建 `assets/vehicle-images.json` 索引
- `data/import-template.csv`：22 列导入模板

**图片组织**：`uploads/cars/<id>/primary.jpg` + `<id>_photo_N.jpg`，构建时经索引合并进页面图库。全流程已用测试批次验证（新增 2 台 / 6 图 /车 / 无重复），测试数据未发布到线上。

## 四、部署与验证（本次完成）

- 直连 github.com 被墙、Clash 代理失效，改用 **GitHub REST API（api.github.com 可达）** 完成推送：blobs → tree → commit → 更新 ref，远端树与本地 2005 文件完全一致（TREE_MATCH_OK）。
- 远端 main = 4860e250，Pages 构建 status: built。
- 线上验证（2026-08-28）：`v5.css?v=1` ✓ ｜ hreflang ✓ ｜ 设计 token ✓ ｜ 车辆详情页+图库 ✓。

## 五、后续建议

1. 用真实车源填 `data/import-template.csv` + 提供车照目录，跑 `vehicle_pipeline.py` 即可批量上新车。
2. 车辆 id 1-6 为占位/未发布，线上从 id 7 起。
3. 旧 v4.css 保留作回滚备份，稳定 1-2 周后可清理。
