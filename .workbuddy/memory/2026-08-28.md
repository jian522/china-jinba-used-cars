# 2026-08-28 jinbacars.com 体检与修复部署

## 线上检查结果
- 托管：GitHub Pages（jian522/china-jinba-used-cars，Fastly CDN，japaneast 边缘），HTTPS 正常
- 站点结构：4 语言（en/zh/ru/ar）× 792 页，154 台已发布车辆，v4 UI

## 发现并修复的两个 Bug（commit c31b2d60）
1. **hreflang 全站丢失**：`scripts/build_v2.py` head() 的 f-string 里写成 `{{hre}}`，
   字面量 `{hre}` 输出到全部 792 个页面，hreflang 多语言alternate 标签完全没渲染。
   修复为 `{hre}` 后重建，en/zh/ru/ar/x-default 全部生效。
2. **图库照片大量未展示**：`assets/vehicle-images.json`（958 张图索引）与
   `data/vehicles.json`（仅 214 张）脱节，多数详情页只显示 1 张图。
   修复：构建时合并图库索引并做文件存在性校验，展示图从 214 → 936 张，
   全部 154 台车图集完整（≥6 张）。

## 遗留风险（未修，需人工处理）
- **41 台车主图文件完全相同**（20 组 sha256 重复，如 car8 == car51），
  属于数据/照片管理问题，不是代码问题。photo-audit 后台已能标记。
- 建议后续：v4.css 未压缩（23KB，可接受）；IndexNow 提交可在每次部署后跑 scripts/submit_indexnow.py。

## 部署验证（全部通过）
- 线上 hreflang 标签 ✓、car10/75 详情页 6 图 ✓、新图 200 ✓、sitemap lastmod 2026-08-28 ✓

## 环境备忘
- Git Bash 进中文路径：用 `cd "$(printf 'D:/<UTF8八进制>')"` 或所有命令带绝对路径
- 构建命令：`"C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe" "D:/二手车出口网站/scripts/build_v2.py"`
- origin remote 带 token；`github` remote 无 token（push 用 origin）
- 注意：data/vehicles.json 含 GitHub token 于 origin URL，不要外传该文件内容
